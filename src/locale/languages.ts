import de from "./de.json";
import en from "./en.json";

/**
 * This list is a list of all available language objects.
 */
export let languages: { locale: string, translations: any }[] = [
    de,
    en
];
